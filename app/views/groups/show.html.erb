<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @group.name %>
</p>
<%  if @users.empty? %>
<p>В этой группе пока ещё нету студентов</p>
<% else %>
<ul>
<% @users.each do |user| %>
  <li><%= link_to user.name, user  %></li>
<%end%>
  <%end%>
</ul>


<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
